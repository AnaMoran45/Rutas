<?php echo View::content(); ?>
    <h3 class="centered">Departamentos</h3>

    <?php echo Html::linkAction('create','Nuevo Departamento', "class='btn green right'") ?>

<table class="centered table striped">
    <tr>
        <thead class="black">
            <th>Id</th>
            <th>Nombre Departamento</th>
            <th>Acciones</th>
        </thead>
        <?php foreach ($ListaDepartamentos->items as $item): ?>
            <tr>
                <td><?php echo $item->id?></td>
                <td><?php echo $item->nombre_departamento ?></td>
                <td>
                    <?php
                    if ($item->habilitado ==1){
                        echo "Habilitado";
                    };
                    if ($item->habilitado ==0){
                        echo "Inhabilitado";
                    }
                    ?>
                </td>
                <td>
                    <?php  echo Html::linkAction("edit/$item->id", "Editar", 'class="btn blue"') ?>
                    <?php  echo Html::linkAction("del/$item->id", "Borrar", 'class=" btn red"') ?>
                </td>
            </tr>
        <?php endforeach ?>
    </tr>
</table>