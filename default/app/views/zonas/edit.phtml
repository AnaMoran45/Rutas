<div class="row section">
    <?php echo View::content();?>
    <?php echo Form::open(); ?>
        <h3 class="center">Editar Zona</h3>

        <div class="col s6">
            <label for="">Nombre Zona
                <?php echo Form::text('zonas.nombre_zona'); ?>
            </label>
        </div>

        <div class="col s6">
            <label for="habilitado">Habilitado
                <?php
                $arreglo = array('Inhabilitado', 'Habilitado');
                 echo Form::select('zonas.habilitado', $arreglo);
                ?>
            </label>
        </div>
        <div class="col s12">
            <?php echo Form::hidden('zonas.id');?>
            <?php echo Form::submit('guardar','class="btn green"'); ?>
            <?php echo Form::reset('Limpiar','class="btn blue'); ?>
            <?php echo Html::linkAction('index', 'Regresar',"class='btn red darken-1'") ?>
        </div>
    <?php echo Form::close(); ?>
</div>